# PIQI in Python
This was just a simple attempt to try and serialize PIQI data using Python and a Pydantic-based data model generated by the [quicktype](https://app.quicktype.io/) web site utilizing the JSON schema in this project.  Just run `python3 piqi_test.py`.
